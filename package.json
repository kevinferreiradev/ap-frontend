{
  "name": "root",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "postinstall": "./postinstall.sh",
    "build:api": "yarn workspace api build",
    "build:admin": "yarn workspace admin build"
  }
}
